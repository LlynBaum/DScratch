# Optimization Ideas

## Switch Tag Operation

Switching from `p` to `h1` for example is a very common operation that the User might use very ofter.
It is possible to do this with the current JS Setps but requires 
`DeleteText` + `InsertElement` + `InsertText` + `DeleteElement`. This is more expansive than ine operation.

Maybe this would be worth. Adding this additional complexity for a bit more speed in the Browser.