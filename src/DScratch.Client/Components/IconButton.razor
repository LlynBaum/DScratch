<button
    class="icon-button @(IsActive ? "active" : "")"
    @onmousedown:preventDefault
    @onclick="OnClick"
    title="@Title"
    disabled="@Disabled">
    @if (Icon.HasValue)
    {
        @((MarkupString)GetIconSvg(Icon.Value))
    }
    else
    {
        @ChildContent
    }
</button>
