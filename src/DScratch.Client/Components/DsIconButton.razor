<button
    class="icon-button @(IsActive ? "active" : "") @CssClass"
    @onmousedown:preventDefault="@PreventDefault"
    @onclick="OnClick"
    title="@Title"
    disabled="@Disabled"
    @attributes="AdditionalAttributes">
    @if (Icon.HasValue)
    {
        <DsIcon Icon="Icon.Value" />
    }
    else
    {
        @ChildContent
    }
</button>
