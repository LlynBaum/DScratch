<button
    class="icon-button @(IsActive ? "active" : "")"
    @onmousedown:preventDefault
    @onclick="OnClick"
    title="@Title"
    disabled="@Disabled">
    @if (Icon.HasValue)
    {
        <DsIcon Icon="Icon.Value" />
    }
    else
    {
        @ChildContent
    }
</button>
